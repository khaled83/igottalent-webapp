{{head-layout}}

{{#if session.isAuthenticated}}
  {{facebook-social-jssdk}}
  {{html-header currentUser=currentUser logout=(action "logout")}}
{{else}}
    Unauthenticated
{{/if}}

{{outlet}}

{{html-footer}}